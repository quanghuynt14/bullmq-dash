# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Polling interval in milliseconds (default: 3000)
POLL_INTERVAL=3000

# Optional: comma-separated list of queue names to monitor
# If not set, queues will be auto-discovered from Redis
QUEUE_NAMES=
